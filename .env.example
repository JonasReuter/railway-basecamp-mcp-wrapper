# -----------------------------------------------------------------------------
# Example environment configuration for the Basecamp MCP wrapper
#
# Duplicate this file as `.env` when running locally or set the values in
# Railway's environment variables dashboard when deploying to the cloud.  All
# variables defined here have corresponding descriptions in the README.
# -----------------------------------------------------------------------------

# Basecamp/37signals OAuth client credentials.  You obtain these from
# https://launchpad.37signals.com/integrations when creating your Basecamp app.
BASECAMP_CLIENT_ID=your-client-id
BASECAMP_CLIENT_SECRET=your-client-secret
BASECAMP_ACCOUNT_ID=your-basecamp-account-id

# User agent string sent with every API request to Basecamp.  Use your
# application name and a contact address so that the Basecamp team can
# identify your integration.
USER_AGENT=railway-basecamp-mcp (you@example.com)

# Public base URL of your deployed Railway service.  Railway assigns
# something like https://your-project-name.up.railway.app at deploy time.
# This value is required because Basecamp's OAuth redirect URI must
# exactly match the value registered in your Basecamp integration settings.
PUBLIC_BASE_URL=https://your-project-name.up.railway.app

# Directory where OAuth and access tokens should be stored.  This directory
# should map to a persistent volume in Railway so that refreshed tokens
# survive restarts and redeploys.  A default of /app/data is provided; mount
# a Railway volume to this path for persistence.
TOKEN_DIR=/app/data

# Filename (inside TOKEN_DIR) used to store the OAuth token JSON.  The
# upstream Basecamp MCP server expects this file to exist after the OAuth
# flow has completed.
TOKEN_FILENAME=oauth_tokens.json
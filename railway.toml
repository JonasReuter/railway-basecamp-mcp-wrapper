[build]
builder = "DOCKERFILE"

[deploy]
# Do not override the start command. Railway will use the CMD from the Dockerfile
# (which invokes uvicorn via bash and expands the PORT environment variable correctly).
restartPolicyType = "ON_FAILURE"
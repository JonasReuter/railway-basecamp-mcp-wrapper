fastapi==0.115.8
uvicorn[standard]==0.30.6
pydantic-settings==2.7.1
requests==2.32.3

# The upstream Basecamp‑MCP‑Server project is cloned directly in the
# Dockerfile rather than installed via pip.  Installing it as a pip
# dependency fails because the repository does not provide a correct
# Python package (missing egg‑info).  See the Dockerfile for the
# clone/install logic.